{
  "name": "crossed-wires",
  "version": "1.0.0",
  "description": "Find the intersection point closest to the central port",
  "scripts": {
    "test": "tsc && jest"
  },
  "author": "IRC",
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.1",
    "prettier": "^2.7.1",
    "typescript": "^4.8.2"
  }
}
